server:
  port: 9192

spring:
  datasource:
    username: root
    password: topik712
    url: jdbc:mysql://localhost:3306/lakeSide_hotel_db
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          format-sql: true
servlet:
  multiport:
   max-file-size: 5MB
   max-request-size: 5MB
   file-size-threshold: 2KB
